# Quarto
.quarto/
_book/

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# R
.Rhistory
.Rdata

/.quarto/

# Material de referência extraído de fontes com copyright (deck do
# professor, Stallings, etc.) — só para preparação do deck Gamma,
# nunca para o repositório público.
assets/slides-fonte/

# PDFs gerados (PT e EN) — evitar binários grandes no repositório;
# regenerar com `quarto render --to pdf` / `quarto render en/ --to pdf`
docs/*.pdf
docs/en/*.pdf
